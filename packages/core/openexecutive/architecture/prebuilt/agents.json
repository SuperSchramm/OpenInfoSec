{
  "section_id": "agents",
  "title": "Agent Council",
  "markdown": "The Executive (`openexecutive.orchestrator.executive.Executive`) is a single coherent persona that fans out to specialist sub-agents via Anthropic tool use. Each specialist subclasses `openexecutive.agents.base.BaseAgent`, carrying a `name`, `domain`, and `model`, and exposes `analyze(...)`, which wraps the question in user-turn context and calls Claude through the per-call provider.\n\nRouting uses one `consult_specialist` tool. Multiple parallel `tool_use` blocks in a single assistant turn fan out concurrently through `router.route_parallel`, each running its own domain-filtered ChromaDB query before the specialist sees the question. The Executive then synthesizes the returned analyses into one reply. Tool choice is not always left to the model's discretion: on iteration 1 of a fresh turn, the call forces `tool_choice={\"type\": \"tool\", \"name\": \"consult_specialist\"}` by default so an on-topic question cannot be answered from the model's own general knowledge on the first pass, unless `router.is_off_topic` matches its narrow allowlist (greetings/small talk, or a meta-question about the tool itself) -- `router.plausibly_on_topic`/`router.SPECIALIST_KEYWORDS` no longer gate this decision (an earlier design that forced only on a keyword match missed real domain-specific queries whose wording didn't hit one) and are retained purely as a logged observability signal. Every later iteration in the same turn reverts to unforced `\"auto\"`, so synthesis after a consult is never blocked from finishing with a text answer.\n\nThe thirteen entries in `SPECIALIST_REGISTRY` (`openexecutive.orchestrator.router`):\n\n| Key | Agent | Domain |\n| --- | --- | --- |\n| `cso` | `StrategyAgent` | strategy |\n| `cfo` | `FinanceAgent` | finance |\n| `chro` | `HRAgent` | hr |\n| `gc` | `LegalAgent` | legal |\n| `coo` | `OperationsAgent` | operations |\n| `cmo` | `MarketingAgent` | marketing |\n| `cpo` | `ProductAgent` | product |\n| `ciso` | `CISOAgent` | security |\n| `cyberops` | `CyberOpsAgent` | security |\n| `grc` | `GRCAgent` | security |\n| `board_comms` | `BoardCommsAgent` | board |\n| `talent` | `TalentAgent` | talent |\n| `triage` | `TriageAgent` | triage |\n\n`triage` is registered here (Council-configurable model/prompt, listed in `SPECIALIST_REGISTRY`) but is **not** reachable via `consult_specialist` — `router.CHAT_CONSULTABLE_SPECIALISTS` excludes it (meta-routing for inbound-event significance, not a domain specialist an operator would consult; its real call path is `alerts/pipeline.py` instantiating `TriageAgent()` directly). A `specialist=\"triage\"` tool call is rejected with an explicit message rather than running the triage prompt against a business question.\n\nModels: specialists default to `claude-sonnet-4-6`; a deep-reasoning tier uses `claude-opus-4-7` (adaptive thinking); routing/fast classification uses `claude-haiku-4-5`. Four Council-only agents (`quality_judge`, `utility_fast`, `research`, `fixture_generator`) are model/prompt-overridable in the UI but not callable via `consult_specialist`.\n\nMost specialists only *advise* — they return analysis the Executive synthesizes. The `talent` specialist (Head of Talent & Executive Search) additionally backs a vertical the Executive can *act* on: `orchestrator.talent_tools` gives the main tool loop read access to the live search pipeline (`list_engagements`, `list_candidates`, `get_candidate`, `match_candidates`) plus two side-effecting tools (`set_candidate_stage`, `start_talent_workflow`), so a chat turn can move a candidate or fire a screen/outreach workflow, not just opine. See the Talent section.\n\nBeyond the talent vertical, the Executive can launch *any* workflow conversationally: `orchestrator.workflow_run_tools` adds `list_workflows` (the launchable catalog with each workflow's inputs) and `run_workflow` (runs a built-in or custom workflow to completion and returns its artifact, checkpointing approval gates as `awaiting_human`). This makes the `/jobs` catalog reachable from chat — see the Workflows section.\n\nThe Executive also backs the **Ask OE** panel's form-fill tier via `orchestrator.form_tools.propose_form_values`: when a chat turn arrives with a `<page_context>` block describing a form on the user's screen, the Executive can propose values for it. The proposal is delivered to the UI as a `form_patch` SSE event and placed into the form as highlighted suggestions — the tool never saves or submits anything; the user reviews and saves manually. The tool is **always** in the tool list (tool definitions sit at the top of the cached prompt prefix, so a conditionally-present tool would invalidate the prompt cache whenever panel and non-panel turns alternate); its description restricts usage to turns that carry a form descriptor.",
  "mermaid": "flowchart TD\n  User([User message]) --> Exec[\"Executive<br/>orchestrator\"]\n  Exec -->|consult_specialist| Cso[\"cso: StrategyAgent\"]\n  Exec -->|consult_specialist| Cfo[\"cfo: FinanceAgent\"]\n  Exec -->|consult_specialist| Gc[\"gc: LegalAgent\"]\n  Exec -->|consult_specialist| More[\"chro / coo / cmo / cpo<br/>board_comms / talent\"]\n  Cso --> Claude[\"Claude (Anthropic)\"]\n  Cfo --> Claude\n  Gc --> Claude\n  More --> Claude\n  Claude --> Exec\n  class User entry\n  class Exec compute\n  class Cso compute\n  class Cfo compute\n  class Gc compute\n  class More compute\n  class Claude external",
  "generated_at": "2026-09-09T00:00:00+00:00"
}
